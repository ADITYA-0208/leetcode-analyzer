# Copy to .env.local for local overrides (optional).
# On Vercel: Project Settings → Environment Variables

# Your Back4App backend URL including /api (no trailing slash)
VITE_API_URL=https://leetcodeanalyzer1-gpte55yn.b4a.run/api
